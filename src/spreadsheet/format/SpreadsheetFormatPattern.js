import SpreadsheetPattern from "./SpreadsheetPattern.js";

export default class SpreadsheetFormatPattern extends SpreadsheetPattern {

    // eslint-disable-next-line
    constructor(pattern) {
        super(pattern);
    }
}